extends wiki
block content
  div(id='tool')
    nav.menu
      a.menu-item.selected(href='#', onclick='return false') 역사
      a.menu-item(href='/w/' + title3) 문서
  h1.title= title
  h2.title!= title2
  div!= content